{
  "name": "scientific-document-intelligence",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["apps/web"],
  "scripts": {
    "dev": "npm run dev --workspace=@scidoc/web",
    "build": "npm run build --workspace=@scidoc/web",
    "lint": "npm run lint --workspace=@scidoc/web",
    "test": "npm run test --workspace=@scidoc/web",
    "typecheck": "npm run typecheck --workspace=@scidoc/web"
  },
  "engines": { "node": ">=20" }
}
