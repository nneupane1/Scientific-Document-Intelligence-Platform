# ADR 0002: SDR JSON is canonical

Status: accepted.

PDF remains immutable visual truth, but PDF cannot represent all recovered scientific semantics. A strict, versioned JSON representation is language-neutral, inspectable, API-friendly, and suitable for validation. HTML, Markdown, LaTeX, searchable PDF, search indexes, and future AI inputs are projections from SDR.
